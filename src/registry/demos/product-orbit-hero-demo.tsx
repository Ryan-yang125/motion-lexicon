"use client";
import { demoText, demoValue, type DemoLocaleProps } from "../demo-locale";
import { ProductOrbitHero, type ProductOrbitHeroItem } from "@/registry/components/product-orbit-hero";
const points: readonly ProductOrbitHeroItem[] = [{ id: "field", label: "01", title: "Field notes", description: "Collect sound, place and weather in one portable record.", tone: "#f1c36b" }, { id: "edit", label: "02", title: "Edit rhythm", description: "Arrange material into a paced visual sequence.", tone: "#8ee3d5" }, { id: "publish", label: "03", title: "Publish issue", description: "Ship a durable page with its source and story intact.", tone: "#ec8060" }, { id: "archive", label: "04", title: "Keep archive", description: "Return to every draft without losing its original context.", tone: "#cbb7ff" }];
export function ProductOrbitHeroDemo({ locale = "en" }: DemoLocaleProps = {}) { const localized = points.map((item, index) => ({ ...item, title: locale === "zh" ? ["现场笔记", "编辑节奏", "发布册页", "保存档案"][index] : item.title })); return <div role="group" aria-label={demoText("product-orbit-hero", locale)} className="mx-auto w-full max-w-[560px]"><ProductOrbitHero items={localized} label={demoValue(locale, "北岸出版工具", "Northline publishing tools")} center={<span className="font-serif text-[30px] leading-none tracking-[-.07em] text-[#f4e5c7]">N</span>} /></div>; }
